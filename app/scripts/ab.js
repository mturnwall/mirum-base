let message = 'Hello World';
let foo = message;
const SET = 1111;

console.log(foo);

// TODO
(() => {
    return {
        say: message + foo,
    };
})();

function function_name (x) {
    var hello;
    x.y = 123;
    if (typeof x === 'undefined') {
        var world = x;
        return hello +  world;
    } else {
        return false;
    }
}
$('test')
    .on('click')
    .on('keyup');
function_name();
