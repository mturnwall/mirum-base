let message = 'Hello World',
    foo = message,
    arr = ['foo', 'bar'],
    obj = {
        foo: 'bar'
    },
    xy = foo !== message;
const SET = 1111;

console.log(foo);

// TODO
(() => {
    return {
        say: message + foo
    };
})();

if (arr) {
    // do something
} else {
    if (!arr) {
        // do something else
    }
}

function function_name(ab) {
    var hello,
        that = this;
    x.y = 123;
    that.y = 456;
    if (typeof x === 'undefined') {
        var world = x;
        return hello +  world;
    } else {
        return false;
    }
}

function blockSpacing() { return true; }

$('test')
    .on('click')
    .on('keyup');
function_name();
